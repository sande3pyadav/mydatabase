kernel exploit > uname -r
running services as root > check netstat -antup and then run command ps -aux | grep 
suid binary checking > find / -perm -u=s -type f 2>/dev/null
sudo right > sudo -l 
misconfigure badly cron > ls -l /etc/cron.d list all running cron > then run find / -2 -type f 2>/dev/null
path hijacking > check path if not set full path then create script > Path=.:${pwd} , export $PATH , echo $PATH
